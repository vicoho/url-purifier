# 网址净化工具 - 产品需求文档 (PRD)

> **版本**：v1.2  
> **日期**：2026-06-08  
> **更新内容**：首屏改为双栏工作台布局、功能说明下沉为底部Tips、深色模式改为graphite配色、输入/输出面板等高  
> **语言**：中文  
> **交付形式**：单页HTML应用（纯前端）

---

## 1. 产品概述

### 1.1 产品背景
在社交媒体、论坛、评论区等场景中，用户分享网址时常常会在URL中穿插文字、表情符号或特殊字符，以规避平台的自动屏蔽机制。例如：

- `https://exa😊mple.com/abc`
- `https://exa mple.com`
- `https://exa-mple.com（请去掉横线）`
- `https://exa...mple.com`

这些被"污染"的网址无法直接点击访问，需要手动清理，体验极差。

### 1.2 产品定位
一款轻量、高效的在线网址净化工具，帮助用户一键还原被穿插文字/表情的原始网址，使其恢复可访问状态。

### 1.3 目标用户
- 社交媒体活跃用户
- 论坛/社区浏览者
- 需要频繁处理分享链接的内容工作者

---

## 2. 核心功能

### 2.1 网址净化（核心功能）

#### 功能描述
自动识别输入文本中的被污染网址，去除穿插其中的多余字符，还原为标准的可访问URL。

#### 支持的污染类型

| 污染类型 | 示例 | 净化结果 |
|---------|------|---------|
| 表情符号穿插 | `https://exa😊mple.com` | `https://example.com` |
| 空格穿插 | `https://exa mple.com` | `https://example.com` |
| 中文字符穿插 | `https://exa测mple.com` | `https://example.com` |
| 多余标点穿插 | `https://exa...mple.com` | `https://example.com` |
| 提示性文字包裹 | `https://example.com（请复制到浏览器）` | `https://example.com` |
| 换行/制表符穿插 | `https://exa\nmple.com` | `https://example.com` |
| 多种污染混合 | `https://exa😊 mple...com` | `https://example.com` |

#### 净化规则

1. **URL结构保留**：保留协议头（`http://` / `https://`）、域名、路径、查询参数、锚点等标准URL结构
2. **非法字符移除**：移除URL主体中的表情符号、中文字符、多余空格、换行符、制表符
3. **保留必要符号**：保留URL中合法的符号（如 `-`、`_`、`/`、`?`、`=`、`&` 等）
4. **智能边界识别**：准确识别URL的起止位置，避免误删URL前后的正常文本
5. **多URL处理**：支持同时识别并净化文本中的多个网址

### 2.2 批量处理

- 支持在输入框中粘贴包含多个被污染网址的长文本
- 自动识别文本中所有符合URL模式的字符串
- 分别净化每个网址，保留非URL文本不变

### 2.3 结果展示

- 清晰展示净化后的网址列表
- 每个网址可一键复制
- 提供"全部复制"功能
- 支持直接点击访问（在新标签页打开）

### 2.4 历史记录

- 本地存储最近20条净化记录
- 每条记录包含：原始文本、净化后的URL列表、处理时间
- 历史记录列表直接显示净化后的URL链接（可点击跳转）
- 支持查看详情（显示原始文本，可一键复制）
- 支持清空历史记录
- 与净化结果以Tab形式切换展示

---

## 3. 用户交互流程

### 3.1 主流程

```
┌─────────────────┐
│   进入网页      │
└────────┬────────┘
         ▼
┌─────────────────┐
│  粘贴污染文本   │
│  （输入框）     │
└────────┬────────┘
         ▼
┌─────────────────┐
│  点击净化按钮   │
│  / 自动触发     │
└────────┬────────┘
         ▼
┌─────────────────┐
│  Tab切换        │
│ [净化结果|历史] │
└────────┬────────┘
         ▼
┌─────────────────┐
│  复制/访问网址  │
└─────────────────┘
```

### 3.2 交互细节

#### 输入阶段
- 提供大文本输入框，支持多行粘贴
- 支持 `Ctrl+V` 直接粘贴触发自动净化（可选）
- 显示输入字符数统计

#### 处理阶段
- 点击"净化"按钮后显示处理中状态（加载动画）
- 处理时间应 < 500ms（普通文本）
- 净化成功后自动切换到「净化结果」Tab

#### 输出阶段（Tab形式展示）

**Tab栏**：
- 「净化结果」Tab：展示本次净化后的网址列表
- 「历史记录」Tab：展示本地存储的历史净化记录
- 首次进入页面时，默认展示「历史记录」Tab（因为尚无净化结果）
- 用户执行净化操作后，自动切换到「净化结果」Tab

**净化结果Tab内容**：
- 以卡片/列表形式展示净化后的每个网址
- 每个网址项包含：
  - 净化后的完整URL（可点击）
  - "复制"按钮
  - "访问"按钮（外部链接图标）
- 提供"复制全部"按钮
- 如果未检测到网址，显示友好提示

**历史记录Tab内容**：
- 以时间倒序展示历史记录列表
- 每条记录包含：
  - 处理时间戳
  - 净化后的URL链接列表（可直接点击跳转，超出宽度显示省略号）
  - 净化出的URL数量
  - "查看详情"按钮（弹窗显示原始文本，支持一键复制）
  - "删除"按钮（删除单条记录）
- 提供"清空全部"按钮（带二次确认）
- 空状态时显示提示"暂无历史记录，快去净化第一个网址吧"

---

## 4. 界面设计规范

### 4.1 整体风格（工作台优先）

- **设计理念**：工作台优先。用户进入页面后应立即看到输入区和结果/历史区，避免先展示大段功能说明。
- **色调**：浅色模式采用暖白纸面风格；深色模式采用干净的 graphite/charcoal 配色，避免棕黑脏感。
- **布局**：桌面端为左右双栏工作台，左侧粘贴污染文本，右侧展示净化结果/历史记录；两个主面板在同一行等高显示。移动端自动堆叠。
- **视觉特征**：
  - 圆角控制在 8px ~ 14px，避免过度卡片化
  - 柔和阴影与清晰边框结合，保证工具界面边界明确
  - 精致的图标（Lucide风格或类似）
  - 功能说明仅作为底部 Tips 出现，不占据首屏主视觉

### 4.2 色彩方案（暖白 + Graphite）

#### 浅色模式（默认）—— Claude经典风格

| 用途 | 色值 | 说明 |
|------|------|------|
| 页面背景 | `#F7F2EA` | 暖白纸面背景 |
| 面板背景 | `#FFFFFF` | 主工作面板 |
| 辅助纸面 | `#FFFDF9` | Tab、卡片、浮层纸面 |
| 输入背景 | `#FFFCF7` | 文本输入区 |
| 主色调 | `#D66B33` | 温暖橙色，主操作按钮 |
| 主色调悬停 | `#B95629` | 深一点的橙色 |
| 主色调浅色 | `#F8E6D8` | hover/focus 辅助底色 |
| 强调色 | `#236252` | 成功、数量标签等辅助强调 |
| 文字主色 | `#211A16` | 深暖黑 |
| 文字次色 | `#6E625A` | 辅助文字 |
| 文字三级 | `#9B8D82` | 占位符、弱提示 |
| 边框色 | `#E7DACB` | 暖灰边框 |
| 分隔线 | `#EFE6DA` | 极浅分隔线 |
| 错误色 | `#BE3A2D` | 柔和红色 |
| 链接色 | `#245FAD` | 清晰蓝色链接 |

#### 深色模式

| 用途 | 色值 | 说明 |
|------|------|------|
| 页面背景 | `#0E1116` | 干净 graphite 暗底 |
| 面板背景 | `#181D26` | 主工作面板 |
| 辅助纸面 | `#141821` | Tab、卡片、浮层纸面 |
| 输入背景 | `#0F131A` | 文本输入区 |
| 主色调 | `#FF8A4C` | 暗色下更清晰的橙色 |
| 主色调悬停 | `#FFA06D` | 亮橙悬停 |
| 主色调浅色 | `#302019` | focus/hover 辅助底色 |
| 强调色 | `#61D6B2` | 成功、数量标签等辅助强调 |
| 文字主色 | `#F4F7FB` | 近白主文字 |
| 文字次色 | `#B7C0CC` | 次级文字 |
| 文字三级 | `#7D8794` | 弱提示 |
| 边框色 | `#2D3543` | 中性暗色边框 |
| 分隔线 | `#232A35` | 暗色分隔线 |
| 错误色 | `#FF7D73` | 暗色下清晰红色 |
| 链接色 | `#8AB8FF` | 暗色下清晰蓝色链接 |

### 4.3 字体规范

- **标题字体**：`"Noto Serif SC", "Source Serif 4", Georgia, serif` —— 优雅的衬线体
- **正文字体**：`"Noto Sans SC", "PingFang SC", -apple-system, BlinkMacSystemFont, sans-serif` —— 清晰易读的无衬线体
- **等宽字体（URL展示）**：`"JetBrains Mono", "SFMono-Regular", Consolas, monospace`
- **页面标题**：24px ~ 34px 响应式，衬线体，font-weight: 700
- **区块标题**：20px，无衬线体，font-weight: 600
- **正文字号**：16px，line-height: 1.6
- **URL展示字号**：14px，等宽字体
- **辅助文字**：14px，次要颜色

### 4.4 页面结构

```
┌─────────────────────────────────────────────────────┐
│ [logo] URL Purifier                                 │
│        网址净化工具                          [主题] │  ← 顶部品牌与主题切换
│                                                     │
├─────────────────────────────────────────────────────┤
│ ┌───────────────────────┐ ┌───────────────────────┐ │
│ │ INPUT        已输入0字 │ │ 净化结果 | 历史记录    │ │
│ │ 粘贴污染文本           │ │                       │ │
│ │ ┌───────────────────┐ │ │ ┌───────────────────┐ │ │
│ │ │ https://exa...    │ │ │ │ 结果卡片/历史记录  │ │ │
│ │ │                   │ │ │ │                   │ │ │
│ │ └───────────────────┘ │ │ └───────────────────┘ │ │
│ │ [一键净化] Ctrl+Enter │ │ [复制/访问/查看详情]   │ │
│ └───────────────────────┘ └───────────────────────┘ │
│        ↑ 左右两个主面板桌面端等高显示                │
├─────────────────────────────────────────────────────┤
│ Tip 01 批量识别 | Tip 02 本地处理 | Tip 03 快捷键   │
├─────────────────────────────────────────────────────┤
│ 使用说明 · 常见问题 · 关于                          │
└─────────────────────────────────────────────────────┘
```

#### Tab设计说明

- Tab栏使用分段按钮样式，当前 Tab 以面板背景和边框突出
- Tab切换有平滑过渡动画
- 净化结果Tab和历史记录Tab内容区高度自适应
- 首屏不展示大段说明文案，说明信息下沉到页面底部 Tips 和弹窗中

### 4.5 动效规范

- **按钮悬停**：轻微上移（translateY -1px）+ 阴影加深，过渡 180ms
- **结果出现**：淡入 + 上滑动画，stagger 间隔 80ms
- **Tab切换**：内容区淡入淡出（opacity + translateX），过渡 250ms
- **复制成功**：按钮文字变为"已复制" + 橙色背景闪烁，2秒后恢复
- **加载状态**：按钮内显示旋转动画
- **卡片悬停**：轻微阴影加深 + 边框色变暖，过渡 150ms

---

## 5. 技术方案

### 5.1 技术栈

- **框架**：纯原生 HTML + CSS + JavaScript（零依赖）
- **无需构建工具**：直接打开 `index.html` 即可运行，无需打包编译
- **兼容性**：现代浏览器（Chrome、Firefox、Safari、Edge 最新两版）

### 5.2 核心算法

#### URL识别
使用正则表达式匹配文本中的URL模式：

```javascript
// 宽松捕获 http(s):// 后的候选片段，允许空格、中文、表情等污染字符先进入净化阶段
const urlRegex = /https?:\/\/[^\n\r<>]+/gi;
```

#### 净化算法

```javascript
function purifyURL(dirtyUrl) {
  // 1. 移除表情符号（Unicode emoji范围）
  // 2. 移除中文字符
  // 3. 移除多余空格、换行、制表符
  // 4. 移除URL中不应该出现的标点符号
  // 5. 保留URL合法字符
  return cleanedUrl;
}
```

#### 多URL提取策略

1. 先通过正则找出所有可能的URL片段
2. 对每个片段应用净化算法
3. 验证净化结果是否为合法URL（通过URL构造函数）
4. 返回有效URL列表

### 5.3 本地存储

- 使用 `localStorage` 存储用户主题偏好
- 使用 `localStorage` 存储最近20条净化记录
- 历史记录数据结构：

```javascript
{
  id: "uuid",
  timestamp: 1717584000000,
  originalText: "原始文本内容...",
  urls: [
    "https://example.com",
    "https://another.com"
  ]
}
```

- 历史记录按时间倒序排列
- 超过20条时自动删除最旧的记录

### 5.4 文件结构

```
url-purifier/
├── index.html          # 主入口文件（HTML结构）
├── src/
│   ├── css/
│   │   └── style.css   # 样式文件（工作台布局、主题、动效、响应式）
│   └── js/
│       └── app.js      # 核心逻辑（URL净化、历史记录、Tab切换、主题切换）
├── docs/
│   └── PRD.md          # 产品需求文档
├── .github/
│   ├── workflows/
│   │   └── static.yml  # GitHub Pages 自动部署配置
│   ├── ISSUE_TEMPLATE/
│   │   ├── bug_report.md      # Bug 报告模板
│   │   └── feature_request.md # 功能请求模板
│   └── PULL_REQUEST_TEMPLATE.md
├── README.md           # 项目说明
├── CONTRIBUTING.md     # 贡献指南
├── LICENSE             # 开源协议
└── .gitignore          # Git 忽略规则
```

---

## 6. 边界情况处理

| 场景 | 处理方式 |
|------|---------|
| 输入为空 | 显示提示"请输入包含网址的文本" |
| 未检测到网址 | 显示提示"未检测到网址，请检查输入" |
| 净化后URL不合法 | 标记为"疑似网址"，仍展示但添加警告样式 |
| 输入文本极长（>10万字） | 显示进度提示，分批处理 |
| 用户粘贴图片/文件 | 忽略非文本内容，提示"请粘贴文本内容" |
| 浏览器不支持 | 显示基础版本提示，建议升级浏览器 |

---

## 7. 非功能需求

### 7.1 性能
- 页面加载时间 < 1s
- 净化处理时间 < 500ms（普通长度文本）
- 支持同时处理50+个URL

### 7.2 可访问性
- 支持键盘操作（Tab导航、Enter触发）
- 按钮和链接有清晰的焦点样式
- 颜色对比度符合WCAG AA标准

### 7.3 安全
- 纯前端处理，不发送任何数据到服务器
- 用户隐私100%本地保护
- 外部链接打开时添加 `rel="noopener noreferrer"`

---

## 8. 未来扩展（可选）

- [ ] 支持更多协议（ftp、magnet、ed2k等）
- [ ] 二维码生成（将净化后的URL转为二维码）
- [ ] 浏览器扩展版本
- [ ] 批量导出功能（导出为txt/json）
- [ ] URL安全检测（检测钓鱼/恶意链接）
- [ ] 多语言支持

---

## 9. 验收标准

- [ ] 用户可粘贴包含被污染网址的文本
- [ ] 点击净化后，正确识别并净化所有网址
- [ ] 净化后的URL可直接点击访问
- [ ] 可复制单个或全部净化结果
- [ ] 深色/浅色主题可正常切换
- [ ] 在主流浏览器中正常运行
- [ ] 所有数据仅在本地处理，不上传服务器
- [ ] 净化成功后自动保存到历史记录
- [ ] 首次进入页面默认展示历史记录Tab
- [ ] 历史记录支持查看详情、删除单条、清空全部
- [ ] 超过20条历史记录时自动清理最旧条目

---

*文档结束*
